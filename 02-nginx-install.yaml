- name: install and run nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
      name: nginx
      state: installed
  -name: stop nginx
    ansible.builtin.service:
      name: nginx
      state: started